#!/usr/bin/env bash

# Rofi Clipboard Widget (Clean Version)
# Requires: clipmenu, clipmenud, rofi

## Run
CM_LAUNCHER=rofi clipmenu -dmenu -i -p "Clipboard" -theme ~/.config/rofi/themes/clipboard.rasi
